# AGENTS.md - Fin Repository Guide

## Overview
Chinese A-stock quantitative trading system with ML-based stock selection and market regime detection.

## External Data Storage (2TB 外接盘)
- 项目内 `external_data/` → 软链接 → `/Volumes/MAC外接/fin_data`（2TB 外接硬盘, 1.8Ti 可用）。
- **规则：所有试验性质的大文件（训练审计 CSV、滚动模型、实验回测结果、临时数据）一律写入 `external_data/`，禁止写进项目磁盘根分区**，避免再次因磁盘占满中断任务（历史教训：15GB 审计 CSV 曾多次撑爆系统盘）。
- **审计 CSV 可复用**: 训练审计数据 (~15GB/个) 是模型对比/IC 分析的重要输入, 应**保存到 `external_data/audit/`** 避免反复重训。`market_ml.py` 已支持 `audit_dir` 参数 (默认与 pkl 同目录, 传 `external_data/audit` 写外接盘, pkl 存绝对路径)。现役审计文件已迁至外接盘, 项目根留有软链接兼容旧引用。
- 生产核心文件（模型 pkl 现役、backtest.py 等源码）留在项目内。

## 探索文件管理 (持久化规则, 2026-08-11)
- **探案脚本一律不落项目根**: gate/explore/train(实验变体)/回测对比/分析脚本（如 `gate_*.py`, `train_*_{variant}.py`, `analyze_*.py`, `run_*backtest*.py`）**必须放** `external_data/explore_night/scripts/`，运行 `PYTHONPATH=. python external_data/explore_night/scripts/xxx.py`（cwd=项目根）。`feature_gate.py`（探案基建）同目录。
- **确认采纳才入项目根**: 只有 gate PASS + 回测提升且决定启用后, 相关代码才以正式名并入原项目。Gate FAIL 的脚本/报告/特征代码一律留在外接盘不落地。
- **探案过程文件留存**: 探案日志/报告/gate JSON/回测对比 JSON 存 `external_data/explore_night/<stage>/`，挂载盘容量充足时尽量留存，避免后续讨论重复计算。**审计 CSV 同理**，如允许保留的审计文件应保存到 `external_data/audit/`。
- **非现役/实验模型 pkl**: 一律 `external_data/models/`；现役 pkl 留项目根。
- **生产文件探案改动必须回滚**: 探案对 `co_compute.py`/`market_ml.py`/`backtest.py` 等生产文件的临时改动（如新增特征函数、训练参数），未确认采纳时**必须回滚**，不准残留探案代码在生产管线（避免拖慢推理/训练、混淆后续开发）。

## Architecture
```
main.py → screen.py → backtest.py
                      ↓
            co_compute.py (feature engineering)
            is_market_ok.py (market regime)
            local_data_cache.py (SQLite cache)
            stock_fetcher_bao.py (BaoStock API)
```

## Key Commands
```bash
# Run full backtest (entry point)
python main.py               # daily: sync data + backtest + today's signals

# Run backtest directly with custom symbols
python -c "import backtest; backtest.run_backtest(['600000', '000001'])"

# Daily data acquisition (interactive menu or explicit tasks)
python get_base_data.py                    # interactive menu
python get_base_data.py --task all         # run all 5 tasks sequentially
python get_base_data.py --task daily --date 2026-08-04   # backfill a specific day
# Tasks: financial (reports), macro (money supply), zzqz (index 000985, needs captcha), market (context parquet), daily (BaoStock->SQLite)

# Run ML model audit
python ml_check.py

# Check trade results
python check_trades.py
```

## Critical Files
- `backtest.py`: Main strategy logic, PyBroker integration (~1300 lines)
- `co_compute.py`: Feature engineering, chip metrics, Z-score standardization
- `is_market_ok.py`: Four-quadrant market regime detection
- `local_data_cache.py`: SQLite data management with auto-migration
- `stock_fetcher_bao.py`: BaoStock API wrapper with retry logic

## Data Flow
1. **Data Sources**: BaoStock API → SQLite cache (`stock_data_cache/`)
2. **Features**: Chip distribution, market breadth, momentum indicators
3. **ML Models**: Two LightGBM models (entry + risk) in `.pkl` files
4. **Regime Detection**: Four scenarios: bottom, opportunity, caution, normal, risk

## Dependencies
- `pybroker`: Backtesting framework (requires `TA-Lib`, `numba`)
- `baostock`: Chinese stock data API
- `lightgbm`: ML models
- `numba`: JIT compilation for performance

## Environment Setup
```bash
# Create virtual environment
python -m venv .venv
source .venv/bin/activate

# Install dependencies (Mac)
pip install -r requirements-mac

# System requirements for TA-Lib (Mac)
brew install ta-lib
```

## Common Pitfalls
1. **PyBroker Logger Bug**: `backtest.py:40-49` patches a typo in PyBroker source
2. **Numba Compilation**: First run slower due to JIT compilation
3. **SQLite WAL Mode**: Data cache uses WAL for concurrent reads
4. **BaoStock Session**: Auto-reconnect on session expiration (error codes 10001001/10001002)
5. **Feature Smoothing**: Dual-channel processing (smoothed for entry, raw for risk)
6. **Lookahead Constraint**: `co_compute.compute_individual_indicators` features are end-of-day only; always trade with `buy_delay>=1` (backtest.py configures 1). Never reuse same-day features for intraday decisions.
7. **feature_contri Valid**: LightGBM `feature_contri` (aliases `feature_contrib`/`fc`/`fp`) IS a real param in 4.6.0 — scales split gain as `gain[i]=max(0,contri[i])*gain[i]`; must list all features in order. market_ml.py uses 0.45 for MKT_FEATURES. Confirmed working.
8. **Market Context Alignment**: training reads `market_context_cache.parquet` (generated by `co_compute.sync_market_context_file`, called from get_base_data.py:591) while backtest computes mkt factors live. Refresh parquet after every data sync so train/infer mkt features stay aligned.
9. **磁盘管理**: 训练审计 CSV 单次可达 15GB (回测 debug CSV 9GB 受 `DEBUG_INFERENCE=1` 控制)。**审计 CSV 是模型对比/IC 分析的重要输入, 应保存到 `external_data/audit/` 复用, 而非删除** (需 `market_ml.py` 支持自定义 audit_dir)。实验产物一律写 `external_data/`, 大文件不入 git (见 .gitignore)。历史教训: 审计 CSV 写在系统盘曾撑爆导致任务中断。
10. **模型加载**: `backtest.py:72-73` 默认加载 `chip_accumulation_v6.pkl` (旧模型)! 现役最优为 `chip_accumulation_v6_newfeat.pkl` + `chip_risk_model_v1_newfeat.pkl`, 需用 `backtest.reload_models(...)` 显式切换。另有实验模型 `chip_*_mktfull.pkl` (大盘特征不降权对比, 非现役)。
 11. **大盘特征降权不可动**: `market_ml.py` 对 MKT_FEATURES 硬编码 `feature_contri=0.45` 是必要正则化。mkt_contri=1.0 实验 (2026-08) 回测 -19.3pp + 回撤恶化 4.6pp, 大盘特征权重越高个股区分越差。可用 `mkt_contri` 参数做对照, 但现役保持 0.45。风控模型无早停跑满迭代, feature_contri 对其无效。
 12. **PyBroker Warmup 机制 (反复踩坑!)**: 回测从 start_date 导入数据, 走完 `warmup` 个**交易日**后才进入交易期 (before_exec/选股逻辑才开始执行)。**若回测区间 < warmup 交易日数, 整个区间被 warmup 吃掉, 不产生任何交易和 Live Signal**。正确设计: `start_date = 交易期起点 - warmup 个交易日` (用 zzqz_df.xlsx 交易日历精确计算, 勿用自然日估算)。见 main.py 的 `trading_days_back()`。
 13. **Live Signal 依赖完整回测**: "今日推荐" (买入+持仓卖出信号) 只在 `run_backtest` 结束后由 `_daily_market_cache` (末个交易日) 输出, 依赖回测循环走到 end_date。短区间/小股票池可能因 `active_ctxs` 为空而不触发 before_exec → 无输出。每日运行用 `main.py` (自动前置 warmup)。
 14. **行业特征 gate 双 FAIL (2026-08)**: `ind_inner_rank`(行业内排名) 与 `rs_20_z` 相关 0.72 = rs_20 的行业内重排, 非新信息; `ind_rank_20/60`(行业排名) 单因子 IC 显著为负 (-0.043, 行业越强后续越差) 且模型内 RankIC 增量仅 +0.0012。两者均未进入回测。教训: (a) 对已存在 rs 特征的排名变换会高度冗余, 先查相关性再训练; (b) GPR 高斯化目标下单因子 IC 普遍为负 (基准特征也负), single_ic 判据失真, 应以"模型内 RankIC 增量 + 不主导"为准; (c) 行业级共享常数特征 (31 行业) 同属 mkt 类, 增益微弱不值得。行业数据 (`industry_data.py`, 外接盘 `external_data/industry/`) 与 RANK_FEATURES 计算代码保留, 当前 `RANK_FEATURES=[]` 防污染。
 15. **feature_gate 双缺陷已修复 (2026-08)**: 原始 `run_gate()` 曾有两处误导判据: (a) `ic_ok` 只测"含新特征的模型绝对 OOS RankIC ≥ 0.02", 而基准模型本身 RankIC 就 ~0.04, 新特征无用也照样 PASS → 已改为"基准(不含新特征) vs 实验(含) 的 **RankIC 增量** ≥ 0.02"; (b) 共线性只查 5 个固定旧特征 (turnover/conc/vp_corr/bias), 不含 rs_20_z, 与 rs 冗余的新特征会漏检 → 已改为扫描**全部基准特征**且阈值 0.95→0.8。教训: gate 的判据必须包含基准对比 (增量而非绝对水平), 共线性必须全特征扫描, 否则冗余特征会静默通过。

## Data Cache Structure
- `stock_data_cache/stock_data.db`: Main metadata + adjust factors
- `stock_data_cache/{symbol}.db`: Per-stock OHLCV data (raw columns with `raw_` prefix)
- `financial_reports_all.csv`: Fundamental data cache
- `zzqz_df.xlsx`: Market index data (CSI 300 equivalent)

## ML Model Features
- Entry model: 29 features (chip distribution + market context) — `chip_accumulation_v6_newfeat.pkl`
- Risk model: 32 features (adds congestion, volatility interaction) — `chip_risk_model_v1_newfeat.pkl`
- Z-score standardization: Cross-sectional by date
- Composite feature: `profit_bias_div_z` (profit vs bias divergence)

## Output Files
- `results/{timestamp}/`: Backtest results directory
- `trades.xlsx`: Trade log
- `orders.xlsx`: Order details
- `ultimate_trade_audit.xlsx`: Entry/exit snapshots with market context
- `global_strategy_audit.csv`: Daily market regime decisions
- `debug_inference_results.csv`: Feature inference debug output
